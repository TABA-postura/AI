from typing import Any, Dict


def compute(landmarks: Dict[str, Any]) -> Dict[str, float]:
    """
    랜드마크 기반으로 각도/거리 등의 메트릭을 계산하는 자리.
    지금은 빈 dict만 반환한다.
    """
    # TODO: 실제 메트릭 계산 로직 추가
    return {}
